import { EnsureModuleLoadedOnce } from './ensure-module-loaded-once';

describe('EnsureModuleLoadedOnce', () => {
  it('should create an instance', () => {
    expect(new EnsureModuleLoadedOnce()).toBeTruthy();
  });
});
